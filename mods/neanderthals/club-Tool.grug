on_spawn() {
    set_tool_name("Club")
    set_tool_buy_gold_value(1)
}

on_use() {
    parent: Human = get_human_parent(me)
    opponent: Human = get_opponent(parent)
    change_human_health(opponent, -8)
}
